<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Darshil Pharma - Medicine Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --pharma-blue: #0d6efd;
            --pharma-light-blue: #e7f1ff;
            --pharma-green: #198754;
            --pharma-red: #dc3545;
            --pharma-warning: #ffc107;
            --pharma-purple: #6f42c1;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            color: #333;
        }
        
        .sidebar {
            min-height: 100vh;
            background: linear-gradient(180deg, var(--pharma-blue) 0%, #0a58ca 100%);
            color: white;
            padding: 0;
            position: fixed;
            width: 250px;
            z-index: 1000;
            box-shadow: 3px 0 10px rgba(0,0,0,0.1);
        }
        
        .main-content {
            margin-left: 250px;
            padding: 20px;
            width: calc(100% - 250px);
        }
        
        .logo-container {
            padding: 20px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            text-align: center;
        }
        
        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            color: white;
            text-decoration: none;
        }
        
        .logo span {
            color: #a5d8ff;
        }
        
        .nav-menu {
            padding: 20px 0;
        }
        
        .nav-item {
            margin-bottom: 5px;
        }
        
        .nav-link {
            color: rgba(255,255,255,0.8);
            padding: 12px 20px;
            display: flex;
            align-items: center;
            transition: all 0.3s;
            text-decoration: none;
        }
        
        .nav-link:hover, .nav-link.active {
            color: white;
            background-color: rgba(255,255,255,0.1);
            border-left: 4px solid white;
        }
        
        .nav-icon {
            width: 24px;
            margin-right: 10px;
            text-align: center;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            margin-bottom: 30px;
            border-bottom: 1px solid #dee2e6;
        }
        
        .page-title {
            font-size: 1.8rem;
            font-weight: 600;
            color: var(--pharma-blue);
        }
        
        .card {
            border-radius: 10px;
            border: none;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            margin-bottom: 25px;
            overflow: hidden;
        }
        
        .card-header {
            background-color: white;
            border-bottom: 1px solid rgba(0,0,0,0.05);
            padding: 15px 20px;
            font-weight: 600;
            color: var(--pharma-blue);
        }
        
        .table-container {
            overflow-x: auto;
        }
        
        .medicine-table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .medicine-table thead {
            background-color: var(--pharma-light-blue);
        }
        
        .medicine-table th {
            padding: 15px;
            text-align: left;
            font-weight: 600;
            color: var(--pharma-blue);
            border-bottom: 2px solid var(--pharma-blue);
        }
        
        .medicine-table td {
            padding: 15px;
            border-bottom: 1px solid #eee;
            vertical-align: middle;
        }
        
        .medicine-table tr:hover {
            background-color: #f8f9fa;
        }
        
        .medicine-img {
            width: 60px;
            height: 60px;
            object-fit: cover;
            border-radius: 5px;
            border: 1px solid #dee2e6;
        }
        
        .badge {
            padding: 5px 10px;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 600;
        }
        
        .badge-expiring {
            background-color: #fff3cd;
            color: #856404;
        }
        
        .badge-expired {
            background-color: #f8d7da;
            color: #721c24;
        }
        
        .badge-valid {
            background-color: #d1e7dd;
            color: #0f5132;
        }
        
        .badge-quantity-high {
            background-color: #d1e7dd;
            color: #0f5132;
        }
        
        .badge-quantity-medium {
            background-color: #fff3cd;
            color: #856404;
        }
        
        .badge-quantity-low {
            background-color: #f8d7da;
            color: #721c24;
        }
        
        .action-buttons {
            display: flex;
            gap: 8px;
        }
        
        .btn-action {
            width: 36px;
            height: 36px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 5px;
            border: none;
            transition: all 0.2s;
        }
        
        .btn-edit {
            background-color: #d1e7ff;
            color: var(--pharma-blue);
        }
        
        .btn-edit:hover {
            background-color: var(--pharma-blue);
            color: white;
        }
        
        .btn-delete {
            background-color: #ffeaea;
            color: var(--pharma-red);
        }
        
        .btn-delete:hover {
            background-color: var(--pharma-red);
            color: white;
        }
        
        .btn-view {
            background-color: #e7f6e9;
            color: var(--pharma-green);
        }
        
        .btn-view:hover {
            background-color: var(--pharma-green);
            color: white;
        }
        
        .btn-add {
            background-color: var(--pharma-blue);
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            font-weight: 500;
            transition: all 0.3s;
        }
        
        .btn-add:hover {
            background-color: #0b5ed7;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 5px 10px rgba(13, 110, 253, 0.2);
        }
        
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .stat-card {
            padding: 20px;
            border-radius: 10px;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .stat-card.total {
            background: linear-gradient(135deg, var(--pharma-blue) 0%, #0a58ca 100%);
        }
        
        .stat-card.expiring {
            background: linear-gradient(135deg, var(--pharma-warning) 0%, #e0a800 100%);
        }
        
        .stat-card.expired {
            background: linear-gradient(135deg, var(--pharma-red) 0%, #c82333 100%);
        }
        
        .stat-card.valid {
            background: linear-gradient(135deg, var(--pharma-green) 0%, #146c43 100%);
        }
        
        .stat-card.quantity {
            background: linear-gradient(135deg, var(--pharma-purple) 0%, #5a32a3 100%);
        }
        
        .stat-value {
            font-size: 2.2rem;
            font-weight: 700;
        }
        
        .stat-label {
            font-size: 0.9rem;
            opacity: 0.9;
        }
        
        .stat-icon {
            font-size: 2.5rem;
            opacity: 0.8;
        }
        
        .search-bar {
            max-width: 300px;
            position: relative;
        }
        
        .search-bar input {
            padding-left: 40px;
            border-radius: 50px;
        }
        
        .search-bar i {
            position: absolute;
            left: 15px;
            top: 12px;
            color: #6c757d;
        }
        
        .quantity-cell {
            text-align: center;
        }
        
        .quantity-value {
            font-weight: 700;
            font-size: 1.1rem;
            margin-bottom: 3px;
        }
        
        .quantity-progress {
            height: 6px;
            background-color: #e9ecef;
            border-radius: 3px;
            overflow: hidden;
            margin-top: 5px;
        }
        
        .quantity-progress-bar {
            height: 100%;
            border-radius: 3px;
        }
        
        .quantity-progress-high {
            background-color: var(--pharma-green);
        }
        
        .quantity-progress-medium {
            background-color: var(--pharma-warning);
        }
        
        .quantity-progress-low {
            background-color: var(--pharma-red);
        }
        
        .footer {
            text-align: center;
            padding: 20px;
            color: #6c757d;
            font-size: 0.9rem;
            margin-top: 30px;
            border-top: 1px solid #dee2e6;
        }
        
        @media (max-width: 992px) {
            .sidebar {
                width: 70px;
            }
            
            .main-content {
                margin-left: 70px;
                width: calc(100% - 70px);
            }
            
            .logo-text, .nav-text {
                display: none;
            }
            
            .logo-container {
                padding: 15px 5px;
            }
            
            .nav-link {
                justify-content: center;
                padding: 15px 5px;
            }
            
            .nav-icon {
                margin-right: 0;
                font-size: 1.2rem;
            }
        }
        
        @media (max-width: 768px) {
            .stats-container {
                grid-template-columns: 1fr;
            }
            
            .header {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .search-bar {
                max-width: 100%;
                width: 100%;
                margin-top: 15px;
            }
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <div class="logo-container">
            <a href="#" class="logo"><i class="fas fa-pills nav-icon"></i> <span class="logo-text">Darshil Pharma</span></a>
        </div>
        
        <div class="nav-menu">
            <div class="nav-item">
                <a href="#" class="nav-link active">
                    <i class="fas fa-table nav-icon"></i>
                    <span class="nav-text">Medicine Inventory</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="/addMedicine" class="nav-link">
                    <i class="fas fa-plus-circle nav-icon"></i>
                    <span class="nav-text">Add Medicine</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="#" class="nav-link">
                    <i class="fas fa-chart-line nav-icon"></i>
                    <span class="nav-text">Analytics</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="#" class="nav-link">
                    <i class="fas fa-boxes nav-icon"></i>
                    <span class="nav-text">Suppliers</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="#" class="nav-link">
                    <i class="fas fa-cogs nav-icon"></i>
                    <span class="nav-text">Settings</span>
                </a>
            </div>
        </div>
        
        <div class="user-info" style="padding: 20px; position: absolute; bottom: 0; width: 100%; border-top: 1px solid rgba(255,255,255,0.1);">
            <div class="d-flex align-items-center">
                <div class="me-3">
                    <i class="fas fa-user-circle" style="font-size: 2rem;"></i>
                </div>
                <div>
                    <div style="font-weight: 600;">Admin User</div>
                    <div style="font-size: 0.8rem; opacity: 0.8;">System Administrator</div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="main-content">
        <div class="header">
            <div>
                <h1 class="page-title">Medicine Inventory</h1>
                <p class="text-muted">Manage all medicines in your pharmacy inventory</p>
            </div>
            
            <div class="search-bar">
                <i class="fas fa-search"></i>
                <input type="text" class="form-control" placeholder="Search medicines...">
            </div>
        </div>
        
        <div class="stats-container">
            <div class="stat-card total">
                <div>
                    <div class="stat-value"><%= allMedicines.length %></div>
                    <div class="stat-label">Total Medicines</div>
                </div>
                <div class="stat-icon">
                    <i class="fas fa-pills"></i>
                </div>
            </div>
            
            <div class="stat-card valid">
                <div>
                    <% 
                        let validCount = 0;
                        allMedicines.forEach(medicine => {
                            const expDate = new Date(medicine.exp_date);
                            const today = new Date();
                            const daysToExpiry = Math.floor((expDate - today) / (1000 * 60 * 60 * 24));
                            if (daysToExpiry >= 30) validCount++;
                        });
                    %>
                    <div class="stat-value"><%= validCount %></div>
                    <div class="stat-label">Valid Medicines</div>
                </div>
                <div class="stat-icon">
                    <i class="fas fa-check-circle"></i>
                </div>
            </div>
            
            <div class="stat-card expiring">
                <div>
                    <% 
                        let expiringCount = 0;
                        allMedicines.forEach(medicine => {
                            const expDate = new Date(medicine.exp_date);
                            const today = new Date();
                            const daysToExpiry = Math.floor((expDate - today) / (1000 * 60 * 60 * 24));
                            if (daysToExpiry >= 0 && daysToExpiry < 30) expiringCount++;
                        });
                    %>
                    <div class="stat-value"><%= expiringCount %></div>
                    <div class="stat-label">Expiring Soon</div>
                </div>
                <div class="stat-icon">
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
            </div>
            
            <div class="stat-card quantity">
                <div>
                    <% 
                        let totalQuantity = 0;
                        allMedicines.forEach(medicine => {
                            totalQuantity += medicine.quantity || 0;
                        });
                    %>
                    <div class="stat-value"><%= totalQuantity %></div>
                    <div class="stat-label">Total Quantity</div>
                </div>
                <div class="stat-icon">
                    <i class="fas fa-cubes"></i>
                </div>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <span>All Medicines</span>
                <a href="/addMedicine" class="btn-add">
                    <i class="fas fa-plus me-2"></i> Add New Medicine
                </a>
            </div>
            
            <div class="card-body">
                <div class="table-container">
                    <table class="medicine-table">
                        <thead>
                            <tr>
                                <th>No.</th>
                                <th>Medicine Name</th>
                                <th>Manufacturer</th>
                                <th>MFG. Date</th>
                                <th>Expire Date</th>
                                <th>Quantity</th>
                                <th>Status</th>
                                <th>Image</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% allMedicines.map((medicine,index) => { 
                                // Determine expiry status based on expiry date
                                const expDate = new Date(medicine.exp_date);
                                const today = new Date();
                                const daysToExpiry = Math.floor((expDate - today) / (1000 * 60 * 60 * 24));
                                let expiryStatusClass = '';
                                let expiryStatusText = '';
                                
                                if (daysToExpiry < 0) {
                                    expiryStatusClass = 'badge-expired';
                                    expiryStatusText = 'Expired';
                                } else if (daysToExpiry < 30) {
                                    expiryStatusClass = 'badge-expiring';
                                    expiryStatusText = 'Expiring Soon';
                                } else {
                                    expiryStatusClass = 'badge-valid';
                                    expiryStatusText = 'Valid';
                                }
                                
                                // Determine quantity status
                                const quantity = medicine.quantity || 0;
                                let quantityBadgeClass = '';
                                let quantityProgressClass = '';
                                
                                if (quantity === 0) {
                                    quantityBadgeClass = 'badge-quantity-low';
                                } else if (quantity <= 10) {
                                    quantityBadgeClass = 'badge-quantity-low';
                                    quantityProgressClass = 'quantity-progress-low';
                                } else if (quantity <= 30) {
                                    quantityBadgeClass = 'badge-quantity-medium';
                                    quantityProgressClass = 'quantity-progress-medium';
                                } else {
                                    quantityBadgeClass = 'badge-quantity-high';
                                    quantityProgressClass = 'quantity-progress-high';
                                }
                                
                                // Calculate progress percentage (max 100 units for visualization)
                                const progressPercentage = Math.min(quantity, 100);
                            %>
                                <tr>
                                    <td><strong><%= index + 1 %></strong></td>
                                    <td>
                                        <strong><%= medicine.name %></strong><br>
                                        <small class="text-muted">Batch: <%= medicine.batch || 'BATCH001' + (index + 1) %></small>
                                    </td>
                                    <td><%= medicine.manufacturer%></td>
                                    <td><%= medicine.mfg_date %></td>
                                    <td>
                                        <%= medicine.exp_date %>
                                        <br>
                                        <small class="text-muted"><%= daysToExpiry >= 0 ? `${daysToExpiry} days left` : `${Math.abs(daysToExpiry)} days ago` %></small>
                                    </td>
                                    <td class="quantity-cell">
                                        <div class="quantity-value"><%= quantity %></div>
                                        <span class="badge <%= quantityBadgeClass %>">
                                            <% if (quantity === 0) { %>
                                                Out of Stock
                                            <% } else if (quantity <= 10) { %>
                                                Low Stock
                                            <% } else if (quantity <= 30) { %>
                                                Medium Stock
                                            <% } else { %>
                                                High Stock
                                            <% } %>
                                        </span>
                                        <div class="quantity-progress">
                                            <div class="quantity-progress-bar " ></div>
                                        </div>
                                    </td>
                                    <td>
                                        <span class="badge <%= expiryStatusClass %>"><%= medicine.status %></span>
                                    </td>
                                    <td>
                                        <img src="<%= medicine.img  %>" alt="<%= medicine.name %>" class="medicine-img">
                                    </td>
                                    <td>
                                        <div class="action-buttons">
                                            <button class="btn-action btn-view" title="View Details">
                                                <i class="fas fa-eye"></i>
                                            </button>
                                            <a href="/editMedicine/<%= medicine.id %>" class="btn-action btn-edit" title="Edit">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <a href="/deleteMedicine/<%= medicine.id %>" class="btn-action btn-delete" title="Delete" onclick="return confirm('Are you sure you want to delete this medicine?');">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            <%}) %>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <div class="card-footer d-flex justify-content-between align-items-center">
                <div class="text-muted">
                    Showing <strong><%= allMedicines.length %></strong> medicines | Total Quantity: <strong><%= totalQuantity %></strong>
                </div>
                <div>
                    <nav aria-label="Page navigation">
                        <ul class="pagination mb-0">
                            <li class="page-item disabled">
                                <a class="page-link" href="#" tabindex="-1">Previous</a>
                            </li>
                            <li class="page-item active"><a class="page-link" href="#">1</a></li>
                            <li class="page-item"><a class="page-link" href="#">2</a></li>
                            <li class="page-item"><a class="page-link" href="#">3</a></li>
                            <li class="page-item">
                                <a class="page-link" href="#">Next</a>
                            </li>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
        
        <div class="footer">
            <p>© 2023 Darshil Pharma. All rights reserved. | Medicine Management System v2.0</p>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Search functionality
            const searchInput = document.querySelector('.search-bar input');
            if (searchInput) {
                searchInput.addEventListener('keyup', function() {
                    const searchTerm = this.value.toLowerCase();
                    const rows = document.querySelectorAll('.medicine-table tbody tr');
                    
                    rows.forEach(row => {
                        const medicineName = row.querySelector('td:nth-child(2)').textContent.toLowerCase();
                        const manufacturer = row.querySelector('td:nth-child(3)').textContent.toLowerCase();
                        const quantity = row.querySelector('td:nth-child(6)').textContent.toLowerCase();
                        
                        if (medicineName.includes(searchTerm) || manufacturer.includes(searchTerm) || quantity.includes(searchTerm)) {
                            row.style.display = '';
                        } else {
                            row.style.display = 'none';
                        }
                    });
                });
            }
            
            // Status badge hover effects
            const badges = document.querySelectorAll('.badge');
            badges.forEach(badge => {
                badge.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-2px)';
                });
                
                badge.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0)';
                });
            });
            
            // Action button hover effects
            const actionButtons = document.querySelectorAll('.btn-action');
            actionButtons.forEach(button => {
                button.addEventListener('mouseenter', function() {
                    this.style.transform = 'scale(1.1)';
                });
                
                button.addEventListener('mouseleave', function() {
                    this.style.transform = 'scale(1)';
                });
            });
            
            // Quantity quick edit functionality
            const quantityCells = document.querySelectorAll('.quantity-cell');
            quantityCells.forEach(cell => {
                cell.addEventListener('click', function(e) {
                    if (e.target.closest('.btn-action')) return; // Don't trigger if clicking action buttons
                    
                    const quantityValue = this.querySelector('.quantity-value');
                    const currentQuantity = parseInt(quantityValue.textContent);
                    
                    // Create input for quick edit
                    const input = document.createElement('input');
                    input.type = 'number';
                    input.min = '0';
                    input.value = currentQuantity;
                    input.className = 'form-control form-control-sm';
                    input.style.width = '80px';
                    input.style.display = 'inline-block';
                    input.style.marginRight = '5px';
                    
                    // Create save button
                    const saveBtn = document.createElement('button');
                    saveBtn.className = 'btn btn-success btn-sm';
                    saveBtn.innerHTML = '<i class="fas fa-check"></i>';
                    saveBtn.style.marginRight = '3px';
                    
                    // Create cancel button
                    const cancelBtn = document.createElement('button');
                    cancelBtn.className = 'btn btn-secondary btn-sm';
                    cancelBtn.innerHTML = '<i class="fas fa-times"></i>';
                    
                    // Store original content
                    const originalContent = this.innerHTML;
                    
                    // Replace cell content with input and buttons
                    this.innerHTML = '';
                    this.appendChild(input);
                    this.appendChild(saveBtn);
                    this.appendChild(cancelBtn);
                    input.focus();
                    
                    // Save button click handler
                    saveBtn.addEventListener('click', function() {
                        const newQuantity = parseInt(input.value) || 0;
                        quantityValue.textContent = newQuantity;
                        
                        // Update the quantity badge and progress bar
                        // In a real application, you would send an AJAX request to update the database
                        alert(`Quantity updated to ${newQuantity}. In a real application, this would save to the database.`);
                        
                        // Restore the cell with updated values
                        // For demo purposes, we'll just reload the original
                        cell.innerHTML = originalContent;
                        // Update the quantity value in the restored content
                        cell.querySelector('.quantity-value').textContent = newQuantity;
                    });
                    
                    // Cancel button click handler
                    cancelBtn.addEventListener('click', function() {
                        cell.innerHTML = originalContent;
                    });
                    
                    // Enter key handler
                    input.addEventListener('keypress', function(e) {
                        if (e.key === 'Enter') {
                            saveBtn.click();
                        }
                    });
                });
            });
            
            // Filter by quantity status
            const filterButtons = document.createElement('div');
            filterButtons.className = 'btn-group mb-3';
            filterButtons.innerHTML = `
                <button class="btn btn-outline-primary btn-sm active" data-filter="all">All</button>
                <button class="btn btn-outline-primary btn-sm" data-filter="high">High Stock</button>
                <button class="btn btn-outline-primary btn-sm" data-filter="medium">Medium Stock</button>
                <button class="btn btn-outline-primary btn-sm" data-filter="low">Low Stock</button>
                <button class="btn btn-outline-primary btn-sm" data-filter="out">Out of Stock</button>
            `;
            
            // Insert filter buttons before the table
            const cardBody = document.querySelector('.card-body');
            const tableContainer = document.querySelector('.table-container');
            cardBody.insertBefore(filterButtons, tableContainer);
            
            // Filter functionality
            const filterBtns = filterButtons.querySelectorAll('button');
            filterBtns.forEach(btn => {
                btn.addEventListener('click', function() {
                    // Remove active class from all buttons
                    filterBtns.forEach(b => b.classList.remove('active'));
                    // Add active class to clicked button
                    this.classList.add('active');
                    
                    const filter = this.getAttribute('data-filter');
                    const rows = document.querySelectorAll('.medicine-table tbody tr');
                    
                    rows.forEach(row => {
                        const quantityBadge = row.querySelector('.quantity-cell .badge');
                        if (!quantityBadge) return;
                        
                        const quantityText = quantityBadge.textContent.toLowerCase();
                        let showRow = false;
                        
                        switch(filter) {
                            case 'all':
                                showRow = true;
                                break;
                            case 'high':
                                showRow = quantityText.includes('high');
                                break;
                            case 'medium':
                                showRow = quantityText.includes('medium');
                                break;
                            case 'low':
                                showRow = quantityText.includes('low') && !quantityText.includes('out of');
                                break;
                            case 'out':
                                showRow = quantityText.includes('out of stock');
                                break;
                        }
                        
                        row.style.display = showRow ? '' : 'none';
                    });
                });
            });
        });
    </script>
</body>
</html>